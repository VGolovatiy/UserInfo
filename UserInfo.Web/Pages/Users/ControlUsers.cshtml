@page "/ControlUsers"
@model ControlUsersModel
@{
}

<h2>Users</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Age</th>
            <th>Card Number</th>
            <th>Photo Path</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.users)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td>@user.Age</td>
                <td>@user.CardNumber</td>
                <td>@user.PhotoPath</td>
                <td>
                    <button onclick="location.href='@Url.Action("Edit", "User", new { id = user.Id })'">Edit</button>
@*                     <form method="post">
                        <input type="hidden" name="handler" value="DeleteUser" />
                        <input type="hidden" name="id" value="@user.Id" />
                        <button type="submit">Delete</button>
                    </form> *@
                                        <form method="post" asp-page-handler="DeleteUser" asp-route-id="@user.Id">
                <button type="submit">Delete</button>
                </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-page="CreateUser" class="btn btn-primary">Add User</a>
